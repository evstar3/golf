#define S s[i+H-1]^=s[i]
H,L,o,i;f(char*s){for(o=0,L=strlen(s),H=L/2;o<L;i=o+=H+L%2)for(;i<o+L/4;i++)S^=S;}

int main(int argc, char *argv[])
{
    for (int i = 1; i < argc; i++)
    {
        char *str = strdup(argv[i]);

        f(str);
        printf("%s\n", str);

        free(str);
    }

    return 0;
}